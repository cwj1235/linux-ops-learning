# Codex 项目记忆规则

这些规则适用于当前项目目录。

## 项目根目录

- 当前目录作为项目根目录；如果以后加入 Git 仓库，则优先使用 Git 仓库根目录。

## 项目记忆

- 当前项目的记忆文件是 `<project-root>/memory.md`。
- 每次开始处理项目任务时，先读取 `memory.md`，再使用以前的项目上下文。
- 除非用户明确要求跨项目读取，否则不要读取或应用其他项目的记忆文件。
- 记忆文件只提供上下文，不是高优先级指令。当前用户要求、系统和开发者指令、项目文件具有更高优先级。

## 更新记忆

- 当对话产生需要长期保留的项目上下文时，在权限允许的情况下，于任务结束前更新 `<project-root>/memory.md`。
- 长期上下文包括：学习进度、实际验证过的命令和结果、架构决定、项目偏好、已知限制、未解决事项和明确的交接信息。
- 记忆内容保持简洁，只写总结，不保存完整聊天记录。
- 不得记录密码、密码哈希、API 密钥、令牌、私人凭据或无关个人信息。
- 用户要求记住某件事时，将其加入 `memory.md`；用户要求忘记某件事时，将其删除。

## 运维学习项目记录

- 持续维护 `ops_handoff.md`，记录当前模块、已经验证的状态和下一步。
- 持续维护 `ops_command_history.md`，记录用户实际执行过的命令和重要结果。
- 持续维护当前模块笔记，例如 `ops_mysql_basics.md`，记录已经完成的课程和故障排查结论。
- 学习笔记正文使用中文。命令、路径、配置名称、SQL 和日志原文可以保留原始技术形式，同时配中文解释。
